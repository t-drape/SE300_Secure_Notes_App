```mermaid
classDiagram
    class Menu {
        +int select_action
        +open(file)
        +save(file)
        +append(file)
        +summarize(file)
        +delete(file)
        +select()
        +act()
    }
```
